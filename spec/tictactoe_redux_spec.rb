require "spec_helper"

RSpec.describe TictactoeRedux do
  it "has a version number" do
    expect(TictactoeRedux::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
